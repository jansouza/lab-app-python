---
kind: Service
apiVersion: v1
metadata:
  name: lab-app-python-service
  namespace: default
  labels:
    app: lab-app-python
spec:
  ports:
    - name: 8080-tcp
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: lab-app-python
